name: Daily Meta Report

on:
  schedule:
    # 15:00 UTC = 21:00 Bangladesh Time (UTC+6)
    - cron: "0 15 * * *"
  workflow_dispatch:

jobs:
  trigger-report:
    runs-on: ubuntu-latest
    steps:
      - name: Trigger Railway report endpoint
        run: |
          curl --fail --show-error --silent "https://cheero-bot-production.up.railway.app/run-report"
